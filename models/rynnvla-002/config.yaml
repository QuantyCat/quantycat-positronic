# Paths — relative to repo root
input_dir: my_data/input_data
work_dir: my_data/training_pipeline
training_output: training_output
task_label: screwdriver
robot: so101

# Must be consistent across preprocessing, training, and inference
chunk_size: 5
action_dim: 6
resolution: 256
his: 2

# Train/validation split
run_validation: true
val_ratio: 0.10

# Training
batch_size: 4
accum_iter: 4
num_workers: 6
epochs: 4
lr: 0.0001
min_lr: 0.00001
clip_grad: 24.0
train_z_loss_weight: 1e-3
fresh_start: true
ckpt_max_keep: 3
save_interval: 2000

# LoRA — trainable params: lora_weight_ + action_head + lm_head
lora_r: 16
lora_alpha: 32

# Inference
checkpoint: /home/caroline/Desktop/fine_tuning/screwdriver_so101/04162026_epoch3
prompt: "Put the screwdriver into the cup"
robot_port: /dev/ttyACM0
robot_id: so101_follower
front_camera_index: /dev/video0
wrist_camera_index: /dev/video4
camera_width: 640
camera_height: 480
camera_fps: 30
gpu: 0
control_period_s: 0.05
max_steps: 0
max_seq_len: 4096
his_mode: 2h_1a_img_both_wrist_state
action_steps: 5
execute_steps_per_inference: 2
deterministic_crop: true
mask_image_logits: false
dropout: 0.0
inference_z_loss_weight: 0.0
