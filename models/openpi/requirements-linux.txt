# openpi model — managed by uv, not pip.
#
# Dependencies are declared in ~/openpi/pyproject.toml and installed by:
#   source models/openpi/run_scripts/setup.sh
#
# This file records the key packages for reference. Do NOT pip-install from here
# directly — use `uv sync` inside ~/openpi instead, which respects locked versions
# and the JAX CUDA wheel overrides in pyproject.toml.
#
# Core ML
jax[cuda12]==0.5.3
flax==0.10.2
torch==2.7.1
transformers==4.53.2
lerobot  # pinned to commit in pyproject.toml uv.sources
openpi-client  # in-workspace package
orbax-checkpoint==0.11.13
safetensors
#
# Robot control (must also be in the rynnvla002 conda env or install separately)
pyserial
#
# Data / utilities
numpy>=1.22.4,<2.0.0
pillow>=11.0.0
opencv-python>=4.10.0.84
einops>=0.8.0
tqdm-loggable>=0.2
wandb>=0.19.1
tyro>=0.9.5
